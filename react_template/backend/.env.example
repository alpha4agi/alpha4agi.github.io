# Server port
PORT=3000

# GitHub Personal Access Token (optional but recommended to increase rate limits)
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Update interval in cron format (default: every 12 hours)
# Current setting: At minute 0 past hour 0 and 12 (twice daily)
UPDATE_SCHEDULE=0 0,12 * * *